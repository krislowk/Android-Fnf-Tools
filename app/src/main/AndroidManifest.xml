<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <application
        android:name=".ZenApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.ZenFNFToolkit"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".SpritesheetConverterActivity" android:exported="false" />
        <activity android:name=".IconGridActivity" android:exported="false" />
        <activity android:name=".GifToSpritesheetActivity" android:exported="false" />
        <activity android:name=".FrameManagerActivity" android:exported="false" />
        <activity android:name=".CrashActivity" android:exported="false" />

    </application>

</manifest>
